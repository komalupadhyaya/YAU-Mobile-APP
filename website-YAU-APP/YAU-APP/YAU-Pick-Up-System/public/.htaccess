RewriteEngine On
RewriteBase /

# If the request is an existing file or folder, serve it normally
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Otherwise, send everything to the SPA entry
RewriteRule . /index.html [L]
